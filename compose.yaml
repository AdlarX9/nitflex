name: Nitflex

services:
    mongodb:
        image: mongo:latest
        restart: always
        ports:
            - "27017:27017"
        volumes:
            - ./mongodb_data:/data/db:rw

volumes:
    mongodb_data:
        driver: local
